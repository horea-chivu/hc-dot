#!/bin/bash

HOME_PREFIX=~/
declare -a DOT_FILES=(.bashrc .bash_aliases .gitconfig .ctags)

# Remove symbolic files
echo "
-- Removing symbolic links
"
for file in ${DOT_FILES[@]}; do
    if [ -e $HOME_PREFIX$file ]; then
        rm $HOME_PREFIX$file ~/.dot_backup
    else
        echo "File does not exist: $file"
    fi
done

rm ~/.config/terminator

echo "
-- Removing .hc-dot directory
"
sudo rm -R ~/.hc-dot
